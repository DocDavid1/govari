# גוב ארי — אודיט + מעבר למשפך ליד (callback‑first)

תאריך: 2026‑09‑09 · מבוצע ע"י: senior product / CRO / reliability / security
מטרה עסקית: להפוך תנועת מטא בתשלום ללידים מוסמכים (שם + טלפון → חוזרים אליהם). **לא** צ'קאאוט.

---

## 1. ארכיטקטורה — לפני השינויים

| רובד | מצב קודם |
|---|---|
| פרונט | HTML/CSS/JS סטטי, RTL, מצב כהה. 9 עמודים. אין פריימוורק. |
| הגשה | `POST /api/orders` (Express serverless ב-Vercel, `api/index.js` עוטף את `server/app.js`). |
| מסד | **אין.** `DATABASE_URL` לא הוגדר ב-Vercel → הזמנות נכתבו ל-`server/data/orders.json` על **דיסק serverless ארעי**. |
| התראות | Resend — **כבוי** (`RESEND_API_KEY` לא הוגדר). המייל פשוט דולג. |
| מדידה | **אין** — לא Meta Pixel, לא GA, לא GTM, בשום עמוד. |
| טופס | `checkout.html` — שם + טלפון + **אימייל (חובה)** + כתובת + תקנון (חובה). `preorder.html` — טופס ששולח ל‑WhatsApp דרך `js/main.js` (ללא שרת). |
| סליקה | `PAYMENT_PROVIDER=none` — אדפטר גנרי לא ממומש. |
| דומיין live | `https://govari-d7u3.vercel.app` (וגם `govari.vercel.app` — דפלוי ישן יותר). |

### ליקוי קריטי (תוקן)
> **כל ליד/הזמנה מהאתר החי אבד בשקט.** `/api/orders` כתב לקובץ JSON על filesystem ארעי של Vercel; ב-cold start הבא הקובץ נמחק. אין מסד, אין מייל → אפס תיעוד. זו הייתה נקודת הכשל מס' 1.

### ליקויים נוספים שנמצאו
- טופס דורש אימייל + כתובת + אישור תקנון — חיכוך גבוה לתנועה קרה.
- `/api/orders` הפנה ל-`order-success.html` גם כשההזמנה לא נשמרה בפועל (הצהרת הצלחה כוזבת).
- אין honeypot / הגנת ספאם על טפסים (מלבד rate-limit על orders).
- אין `/api/health` אמיתי — `/health` החזיר רק דגלי email/payment.
- אין כותרות אבטחה (`X-Content-Type-Options` וכו').
- `tel:` לא אחיד: 17× `tel:0536813013`, 6× `tel:+972...`.
- ניסוח "ללא תשלום / 0 ₪" חזר ~7 פעמים בעמוד הבית — רעש.
- טענות שיווק לא מאומתות (ר' §4). Placeholders משפטיים `[להשלמה]` (ר' §5).
- אי-התאמת ניסוח תרומה (ר' §3).

---

## 2. מספרי טלפון / מחירים / אימיילים / CTA / WhatsApp — ממצאי סריקה

| פריט | ערך שנמצא | הערה |
|---|---|---|
| טלפון | **053‑6813013** בכל האתר (עשרות מופעים), וגם ב-`server/templates.js`, `.env.example`. | **דורש אישור דוד לפני קמפיין.** לא הוחלף — רק אוחד ל-`tel:+972536813013`. |
| WhatsApp | `972536813013` (עקבי בכל מקום). | תואם לטלפון. |
| מחיר | **1,090 ₪** (15+ מופעים, עקבי). "מבצע השקה". | אין מחיר עוגן ("מחיר מלא"). מוצג לשקיפות, לא כ-CTA. |
| אימייל | `davidazulay75@gmail.com` — ב-`content.md`, `DEPLOY.md`, 3 עמודים משפטיים, `templates.js`, `.env.example`. | זהו גם `OWNER_EMAIL` (יעד התראות). `meyer2615@gmail.com` שב-CLAUDE.md הוא משתמש Claude Code — לא בשימוש באתר. |
| CTA ראשי (קודם) | "הזמנה מראש" / "להזמנה — ללא תשלום · 1,090₪" / "לשריון הזמנה". | הוחלף ל-"רוצה לשמוע פרטים?" / "אני רוצה שיחזרו אליי". |
| טפסים | `checkout.html` (הזמנה מלאה), `preorder.html` (→WhatsApp). | נוסף טופס ליד קצר (שם+טלפון+עיר) ב-`index` ו-`preorder`, מפנה ל-`/api/leads`. |
| מעקב | אין. | נוסף `site/js/track.js` — Pixel (מזהה מ-config), לכידת UTM/fbclid, אירועי משפך, CAPI צד שרת. |

---

## 3. אי-התאמת "תרומה" — §19 בבריף

נמצאו **שתי גרסאות** של הניסוח:
- `index.html` (כותרת מודגשת): "10% **מכל רכישה**"
- `index.html` (רשימת נקודות) + `js/main.js` (toast): "10% **מהרווח** על כל רכישה"

**פעולה שבוצעה:** אוחד זמנית ל-**"10% מהרווח על כל רכישה"** בכל המופעים (הניסוח השמרן/הבטוח משפטית).
**דרוש אישור דוד:** מהו הניסוח הנכון? "מהרווח" ≠ "מהמחזור/מכל רכישה" — הבדל מהותי בהתחייבות. גם: לאיזו עמותה בדיוק, והאם יש אסמכתה. עד לאישור — עדיף לרכך ל"חלק מהרווח נתרם" או להסיר את האחוז המספרי.

---

## 4. טענות שיווק — §18 בבריף (דרוש אישור דוד / מסמך יצרן)

`content/content.md` מסמן את כל המפרט כ"לאשר/להשלים". אין מסמך יצרן קריא במאגר (`画册-2026-中性.pdf` קיים אך לא אומת). לכן — **כל הטענות הבאות טעונות אישור לפני קמפיין ממומן**:

| טענה | מופיעה ב | סטטוס |
|---|---|---|
| "הדגם היחיד שמצלם ומתריע 24/7" | (קודם) index, preorder, footers | **הוסר** — סופרלטיב לא בר-הוכחה, סיכון משפטי. הוחלף ב"מצלמת רכב חכמה שמלווה אותך 24/7". |
| "2K Ultra HD" | index, features, preorder | **לא מאומת.** `content.md`: רזולוציה = "לאשר/להשלים". (Changelog קודם דיבר גם על 4K.) |
| "360° · 170° לכל עדשה" | features (`170°`), index (`360°`) | 360° מופיע כיתרון מאושר ב-`content.md`. **"170° לכל עדשה" לא מאומת.** |
| "כיסוי מלא בלי נקודות עיוורון" | index, features | תלוי במפרט העדשות — **לא מאומת.** |
| "גיבוי אוטומטי לענן" | index, features, app | **לא מאומת.** |
| "ראיית לילה" | features, app, preorder | **לא מאומת.** |
| "זיהוי תנועה / חיישן פגיעה (G)" | index, features, app | **לא מאומת** (מופיע כשאלה פתוחה ב-`content.md`). |
| "התראה בזמן אמת" | הרבה מקומות | סביר למצלמת 4G, אך **לא מאומת.** |
| "אפליקציה בעברית · iOS+Android" | הרבה מקומות | ✅ מופיע כיתרון מאושר ב-`content.md`. |
| "חיבור 4G מובנה" | הרבה מקומות | ✅ מאושר ב-`content.md`. |
| "איתור על מפות גוגל / היסטוריית מסלול / גידור גיאוגרפי" | index, app | "איתור על מפות גוגל" מאושר ב-`content.md`. גידור גיאוגרפי/היסטוריה — **לא מאומת.** |
| "הקלטה רציפה (Loop)" | הרבה מקומות | ✅ מאושר ב-`content.md`. |
| "4 ערוצים" | הכול | ✅ מאושר ב-`content.md`. |
| "10% מהרווח ללוחמים/נפגעי צה"ל דרך עמותות רשמיות" | index, main.js | **לא מאומת** — שם עמותה + אסמכתה (ר' §3). |
| "כמות מוגבלת בהשקה" / "שנחת השבוע" | (קודם) preorder | **הוסר** — דחיפות לא מאומתת. |

**המלצה:** לפני הפעלת קמפיין — דוד מאשר כל שורה מול קטלוג היצרן, או שמנסחים בזהירות ("תומך 4G", "כולל ראיית לילה בכפוף לדגם"). מטא פוסלת מודעות עם טענות לא מבוססות.

---

## 5. Placeholders משפטיים — §20 בבריף (אין להמציא — צ'קליסט לדוד + עו"ד)

### `terms.html`
- [ ] ח.פ. / ע.מ. של העסק — סעיף 1 (`ח.פ. [להשלמה]`)
- [ ] כתובת רשומה — סעיף 1 (`מרחוב [להשלמה]`)
- [ ] זמן אספקה/התקנה משוער — סעיף 4.3 (`[להשלמה] ימי עסקים`)
- [ ] תקופת אחריות — סעיף 6 (`[להשלמה] חודשים`)
- [ ] מחוז שיפוט — סעיף 12 (`במחוז [להשלמה]`)
- [ ] קישור למדיניות פרטיות — סעיף 8 (`[להשלמה — קישור]`) — הקישור קיים ב-footer, יש להשלים בגוף הסעיף
- [ ] "מחיר מלא" לעוגן — סעיף 3 מזכיר "מבצע לזמן מוגבל" בלי מחיר עוגן

### `privacy.html`
- [ ] שם ופרטי ממונה פרטיות — סעיף 9 (`[להשלמה]`)
- [ ] האם נדרש רישום מאגר מידע (בהערת ה-muted-note)
- [ ] ניסוח "ספק סליקה מאובטח" — כרגע מדבר על תשלום; בשלב הליד אין תשלום. יש להתאים כשמוגדרת סליקה, או לרכך.

### `accessibility.html`
- [ ] שם רכז/ת נגישות — סעיף 5 (`[שם — להשלמה]`)
- [ ] תאריך מבדק נגישות מקצועי (מומלץ לפני פרסום)

### מודל עסקי מול טקסט משפטי
העמודים המשפטיים מנוסחים סביב **רכישה + סליקה**. המשפך הנוכחי הוא **ליד ללא תשלום**. לפני קמפיין:
- להוסיף פסקה קצרה: "השארת פרטים באתר אינה הזמנה או התחייבות לרכישה ואינה כרוכה בתשלום. עסקה נכרתת רק לאחר תיאום, אישור הלקוח וביצוע תשלום."
- לוודא שאף עמוד לא מצהיר שבוצע תשלום כשלא בוצע (בוצע: `order-success.html` מבחין בין "התקבל" ל"שולם"; דף הצלחת הליד לא מזכיר תשלום כלל).
- **טקסט משפטי טעון בדיקת עו"ד לפני העלאה לאוויר.** אינני עו"ד וזה אינו ייעוץ משפטי.

---

## 6. מה שונה בפועל — ר' `content/changelog.md` ו-`DEPLOY.md`

- נוסף צינור לידים עמיד: `POST /api/leads` → PostgreSQL (Supabase) בטרנזקציה אחת → תשובת הצלחה. התראות (מייל/CAPI/Sheet) דרך **outbox** עם ניסיונות חוזרים — כשל התראה לא מוחק ליד.
- ניסיון חירום (מייל ישיר) אם המסד נופל; אם גם זה נכשל — הודעת אמת + WhatsApp/טלפון עם הפרטים שהוקלדו.
- Frontend מובייל-קודם: הירו עם טופס ליד קצר (שם+טלפון+עיר), סקשן "3 צעדים", רצועת CTA דביקה, פיקסל + לכידת ייחוס.
- `/admin/leads` מוגן ב-Basic Auth (או דשבורד Supabase), עם ייצוא CSV.
- `/api/health`, כותרות אבטחה, honeypot, rate-limit.
- אימייל בצ'קאאוט → אופציונלי. אין תקנון-חובה במסלול הליד.

## 7. מה עדיין צריך מדוד — ר' `DEPLOY.md` §"ערכים שצריך לספק"
Supabase `DATABASE_URL`, `RESEND_API_KEY` + דומיין מאומת, `META_PIXEL_ID` + `META_CAPI_TOKEN`, `ADMIN_USER/PASSWORD`, `IP_HASH_SALT`, אישור טלפון 053-6813013, אישור טענות §4, השלמת §5, הכרעת §3.
